section.user_info
  h1.user_name
    - if current_user.image?
      = image_tag current_user.image.thumb100.url
    - else
      i.fa.fa-user
    = current_user.username

section.tweet_form
  = form_for(@tweet) do |f|
    = render 'shared/error_messages', object: f.object
    .field
      = f.text_area :content, placeholder: "ツイート内容を入力"
    = f.submit "ツイート", class: "btn btn-warning"
